[tool.uv.workspace]
members = ["repo", "signer"]

[tool.uv.sources]
securesystemslib = { git = "https://github.com/jku/securesystemslib.git", branch = "pkcs-switcheroo" }

[dependency-groups]
dev = [
  "tox",
  "tox-uv",
]
uv = ["uv"] # setup-uv uses uv.lock to pin uv
